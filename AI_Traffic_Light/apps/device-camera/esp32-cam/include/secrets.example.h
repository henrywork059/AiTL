#pragma once

// Copy this file to secrets.h. The real secrets.h stays local and is ignored by Git.
#define AITL_WIFI_SSID "YOUR_2_4_GHZ_WIFI_NAME"
#define AITL_WIFI_PASSWORD "YOUR_WIFI_PASSWORD"

// Optional hostname visible on the local network.
#define AITL_DEVICE_HOSTNAME "aitl-cam-01"

// V036 is PC-initiated: there is intentionally no PC/server IP here.
// Start PC Studio, enter the ESP's private-LAN IPv4 address, then Connect.
