# PC Studio Function List — V034 highlights

## Camera

- private-LAN ESP status/control connection;
- idle Connect with zero image transfer;
- PC-owned OV2640 settings;
- target stream FPS 1–30;
- persistent low-latency ESP MJPEG ingestion;
- automatic stream reconnect;
- measured FPS/frame interval/transport counters;
- backend MJPEG Camera Sources preview;
- simulation pause/resume of physical image transport;
- legacy raw JPEG/PNG upload compatibility.

Existing inference, dataset/training, zones, tracking/analytics, signal simulation, network experiments and evidence remain available.

Independent simultaneous per-camera buffers and physical/public-road signal control are not implemented.
