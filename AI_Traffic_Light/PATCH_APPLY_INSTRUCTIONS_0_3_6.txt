AiTL V036 full reviewed multi-ESP patch (R6 - warmup-vectored ESP TCP send repair)

1. Extract this ZIP at the repository root so AI_Traffic_Light/ overlays the existing project.
2. Do not delete datasets, outputs, models, config runtime JSON, .venv, node_modules, or other local runtime/user data.
3. From AI_Traffic_Light run:

   .\scripts\update_test_run.ps1 -SkipUpdate

The V036 runner automatically invokes scripts/apply_v036_full_patch.ps1 before project-structure validation. That finalizer safely adds/repairs the 0_3_6 CHANGELOG section on the existing full historical CHANGELOG.md and synchronizes the frontend project-version source.

Candidate remains 0_3_6. passed_baseline remains 0_2_4 until explicit owner acceptance.

IMPORTANT HARDWARE STEP FOR R6:
- Reflash each ESP32-CAM with the included V036 Arduino/PlatformIO firmware.
- R6 replaces R5's repeated 250 ms abort/reconnect loop with vectored MSG_DONTWAIT writes and a bounded per-connection warm-up before tighter steady-state limits.
- The wire protocol remains aitl-tcp-jpeg-v1; no PC protocol migration is required.
- Start at 240 x 176 or 320 x 240, JPEG quality 14, 15 FPS.
- Serial boot should identify: AiTL V036 R6 warmup-vectored ESP32-CAM node.
- Healthy operation should keep client=on, failures stable, frame counts increasing, and frame age low.
