AiTL V036 full reviewed multi-ESP patch (R4 - non-blocking ESP TCP send repair)

1. Extract this ZIP at the repository root so AI_Traffic_Light/ overlays the existing project.
2. Do not delete datasets, outputs, models, config runtime JSON, .venv, node_modules, or other local runtime/user data.
3. From AI_Traffic_Light run:

   .\scripts\update_test_run.ps1 -SkipUpdate

The V036 runner now automatically invokes scripts/apply_v036_full_patch.ps1 before project-structure validation. That finalizer safely adds/repairs the 0_3_6 CHANGELOG section on the existing full historical CHANGELOG.md and synchronizes the frontend project-version source.

You may also run the finalizer manually first; it is idempotent:

   .\scripts\apply_v036_full_patch.ps1

Candidate remains 0_3_6. passed_baseline remains 0_2_4 until explicit owner acceptance.

IMPORTANT HARDWARE STEP FOR R4:
- Reflash each ESP32-CAM with the included V036 Arduino/PlatformIO firmware. R4 changes the ESP send loop; PC-only extraction cannot apply that firmware repair.
- Start testing at 240 x 176 or 320 x 240, JPEG quality 14, 15 FPS. Healthy serial `send=` values should normally be small rather than repeatedly hundreds of milliseconds.
