# Samples

Use this folder for sample images, videos, and fake prediction JSON files.

Do not commit large datasets to GitHub.

Recommended structure:

```text
samples/
  images/
  videos/
  predictions/
```

For real datasets, use:

```text
datasets/raw/
datasets/processed/
```

and keep large files outside Git or use Git LFS later.
