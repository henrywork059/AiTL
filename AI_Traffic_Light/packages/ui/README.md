# Shared UI Package Placeholder

This folder is reserved for reusable GUI components.

Later, move common frontend components here so the trainer/reviewer/viewer pages do not duplicate code.

Recommended components:

- DetectionViewer
- BoundingBoxOverlay
- ZoneOverlay
- DetectionTable
- TrafficLightSimulator
- CameraStatusPanel
- DatasetCapturePanel
- ModelStatusPanel

In version 1, these components live directly inside `apps/pc-studio/frontend/src/components` for simplicity.
